function pow(x, y) {
  let result = x;

  for (let i = 1; i < y; i++) {
    result *= x;
  }

  return result;
}

let x = prompt("2", '');
let y = prompt("3", '');

if (y < 1) {
  alert(`Степінь {$y } не підтримується, використовуйте натуральне число`);
} else {
  alert( pow(x, y) );
}